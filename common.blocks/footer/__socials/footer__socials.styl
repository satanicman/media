.footer__socials
  margin-bottom 25px
  .socials__list
    padding 60px 0
    border-bottom 1px solid rgba(#fff, .1)
    @media (min-width $screen-lg)
      display flex
      justify-content space-between
      align-items center
